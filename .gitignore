__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
.env
.venv/
venv/

# Runtime file storage — never belongs in version control. These are a
# student's actual uploaded documents (or, in dev, whatever test files
# happen to accumulate locally while running the app or the test suite).
uploads/
